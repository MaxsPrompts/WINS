# Server Configuration
PORT=3000
NODE_ENV=development

# n8n Integration
N8N_GUIDE_GENERATION_WEBHOOK_URL="YOUR_N8N_WEBHOOK_URL_HERE"
# N8N_API_KEY="YOUR_N8N_API_KEY_IF_NEEDED" # Uncomment if your n8n webhook is protected

# Database (placeholders, will be managed by Engineer 3)
DB_HOST=localhost
DB_PORT=5432
DB_USER=admin
DB_PASSWORD=secret
DB_NAME=wins_db
DATABASE_URL="postgresql://${DB_USER}:${DB_PASSWORD}@${DB_HOST}:${DB_PORT}/${DB_NAME}?schema=public"

# JWT Authentication (placeholders, will be managed by Engineer 4)
JWT_SECRET="YOUR_SUPER_SECRET_JWT_KEY"
JWT_EXPIRES_IN="1h"
