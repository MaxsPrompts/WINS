# Frontend Environment Variables
# For Vercel, you might name this .env.local (which is gitignored by default)
# Duplicate this file as .env.local (or .env) and fill in your local values.
# DO NOT COMMIT .ENV OR .ENV.LOCAL FILES TO VERSION CONTROL.

# API Configuration
NEXT_PUBLIC_API_BASE_URL=http://localhost:3001/api # URL of your local backend

# Auth0 Configuration (Development Tenant)
NEXT_PUBLIC_AUTH0_DOMAIN=your-dev-domain.auth0.com
NEXT_PUBLIC_AUTH0_CLIENT_ID=your_frontend_auth0_client_id
NEXT_PUBLIC_AUTH0_REDIRECT_URI=http://localhost:3000 # Your frontend's default redirect URI
NEXT_PUBLIC_AUTH0_AUDIENCE=your_api_audience # Same as backend's AUTH0_AUDIENCE

# Add other necessary public environment variables here
# Prefix with NEXT_PUBLIC_ for them to be exposed to the browser

# Example for Vercel deployment (these would be set in Vercel's UI, not here for local)
# VERCEL_URL=your-dev-instance.vercel.app
